import React, { Component } from 'react';
import "./sections.css";

export default class Sections extends Component {
    render() {
        return(
            <div>

            </div>
        )
    }
}